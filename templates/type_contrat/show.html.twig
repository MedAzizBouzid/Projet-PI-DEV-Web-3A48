{% extends 'back/base.html.twig' %}

{% block title %}TypeContrat{% endblock %}

{% block body %}
    <h1>TypeContrat</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_contrat.id }}</td>
            </tr>
            <tr>
                <th>TypeC</th>
                <td>{{ type_contrat.typeC }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_type_contrat_index') }}">back to list</a>

    <a href="{{ path('app_type_contrat_edit', {'id': type_contrat.id}) }}">edit</a>

    {{ include('type_contrat/_delete_form.html.twig') }}
{% endblock %}
