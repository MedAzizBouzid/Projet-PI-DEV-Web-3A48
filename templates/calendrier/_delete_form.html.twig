<form method="post" action="{{ path('app_calendrier_delete', {'id': calendrier.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ calendrier.id) }}">
    <button class="btn">Delete</button>
</form>
