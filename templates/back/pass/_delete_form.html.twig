<form method="post" action="{{ path('app_pass_delete', {'id': pass.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ pass.id) }}">
    <button class="btn">Delete</button>
</form>
