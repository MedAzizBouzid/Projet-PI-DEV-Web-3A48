{% extends 'back/base.html.twig' %}


{% block body %}
     <div class="row my-4">
       <div class="text-center"> <h1> Promotion disponnible </h1> </div>
       <br>
                    {% for promotion in promotion %}
        <div class="col-xl-6 mb-4">
            <div class="bg-secondary rounded h-100 p-4">
                <h6 class="mb-4">       {{ promotion.id }}</h6>
                <div class="owl-carousel testimonial-carousel">
                    <div class="testimonial-item text-center">
                        {# <img class="img-fluid rounded-circle mx-auto mb-4" src="img/testimonial-1.jpg" style="width: 100px; height: 100px;"> #}
                        <h5 class="mb-1">   {{ promotion.pourcentage }}%</h5>
                        <p> Promo End at:     {{ promotion.dateFin ? promotion.dateFin|date('Y-m-d') : '' }} </p>
                   <p>
                          <a href="{{ path('app_promotion_edit', {'id': promotion.id}) }}"><i class="bi bi-pencil-square"></i></a>
                                              <a href= "{{ path('app_promotion_delete', {'id': promotion.id}) }}" ><i class="bi bi-trash-fill"></i></a>
                                              
                  </div>

                </div>
            </div>
        </div>
        
{% endfor %}
  <a href="{{ path('app_promotion_new') }}">Create new</a>
    </div>



      <!-- Table Start -->
            <div class="row vh-100 bg-secondary rounded align-items-center justify-content-center mx-0">
            <div class="container-fluid pt-4 px-4" style="bottom:0;">
           <h1> Liste Abonnement </h1>
                {# <div class="card-body"> #}
                                <div class="table-responsive">
                        
                                    <table class="table">
                                    <thead>
                                        <tr>
                                           <th>Id</th>
                                              <th> Pourcentage</th>
                                             
                                           <th>Date fin</th>
                                                      <th> Actilons</th>
                                 <tbody> 
                                    {% for promotion in promotion %}
                                            <tr>
                                                 <td>{{ promotion.id }}</td>
                                             
                                                 <td>{{ promotion.pourcentage   }}  </td>   
                                       <td>   {{ promotion.dateFin ? promotion.dateFin|date('Y-m-d') : '' }}  </td> 
                                           
                         <td>   <a href="{{ path('app_promotion_edit', {'id': promotion.id}) }}"><i class="bi bi-pencil-square"></i></a>
                                              <a href= "{{ path('app_promotion_delete', {'id': promotion.id}) }}" ><i class="bi bi-trash-fill"></i></a>
                                                  

                                                         </td> 
                                               
                                             </tr>
                                                       
                                          {% else %}
                                                      <tr>
                                                     <td colspan="6">no records found</td>
                                                      </tr>
                                          {% endfor %}
                          
                                      </tbody>
                                 </table>
                                                     <a href="{{ path('app_promotion_new') }}">Create new</a>
                                               <br>
                                                 
                                            
                                         

    
                                
                                 
                                      
                            </div>
                            </div>
            <!-- Table End -->
                                 
                                      
                         
        

            
    {% endblock %}
