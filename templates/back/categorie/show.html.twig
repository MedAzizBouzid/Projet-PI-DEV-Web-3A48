

            {% extends 'back/base.html.twig' %}

{% block title %}Catégorie{% endblock %}

{% block body %}
    <h1>Catégorie</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ categorie.id }}</td>
            </tr>
            <tr>
               <th>Type</th>
                <td>{{ categorie.Type }}</td>
            </tr>
            
        </tbody>
    </table>

    <a href="{{ path('app_categorie_index_back') }}"><button class=" btn btn-info">Retour </button></a>

    <a href="{{ path('app_categorie_edit', {'id': categorie.id}) }}"><button class=" btn btn-success">Modifier</button></a>

    {{ include('back/categorie/_delete_form.html.twig') }}
{% endblock %}
