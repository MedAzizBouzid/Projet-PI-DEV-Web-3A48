{% extends 'back/base.html.twig' %}


{% block body %}
     <div class="row my-4">
       <div class="text-center"> <h1> Promotion disponnible </h1> </div>
       <br>
                    {% for promotion in categories %}
        <div class="col-xl-6 mb-4">
            <div class="bg-secondary rounded h-100 p-4">
                <h6 class="mb-4">       {{ promotion.id }}</h6>
                <div class="owl-carousel testimonial-carousel">
                    <div class="testimonial-item text-center">
                        {# <img class="img-fluid rounded-circle mx-auto mb-4" src="img/testimonial-1.jpg" style="width: 100px; height: 100px;"> #}
                        <h5 class="mb-1">   {{ promotion.name }} </h5>
                          <p>     {{ promotion.description }} </p>

                   <p>
                          <a href="{{ path('app_categorie_edit', {'id': promotion.id}) }}"><i class="bi bi-pencil-square"></i></a>
                                              <a href= "{{ path('app_categorie_delete', {'id': promotion.id}) }}" ><i class="bi bi-trash-fill"></i></a>
                                              
                  </div>

                </div>
            </div>
        </div>
        
{% endfor %}
  <a href="{{ path('app_categorie_new') }}">Create new</a>
    </div>



 
                                 
                                      
                         
        

            
    {% endblock %}