{% extends 'front/baseF.html.twig' %}


{% block body %}
 <!-- Breadcrumb Section Begin -->
    <section class="breadcrumb-section set-bg" data-setbg="{{ asset('assets/front/img/breadcrumb-bg.jpg')}}">
        <div class="container">
            <div class="row">
                <div class="col-lg-12 text-center">
                    <div class="breadcrumb-text">
                        <h2>account detail</h2>
                        
                    </div>
                </div>
            </div>
        </div>
    </section>
    <!-- Breadcrumb Section End -->

    <!-- Class Details Section Begin -->
   <section class="contact-section spad">
        <div class="container">
            <div class="row">
                <div class="col-lg-6">
                    
                    <div class="contact-widget">
                        {# <div class="cd-trainer-pic"> #}
                                        <img src="{{asset('uploads/'~ user.image )}}" alt="" style="width:1200px; hight:200px;" >
                                    {# </div> #}
                    </div>
                </div>
                <div class="col-lg-6">
                    <div class="leave-comment">
                        {{ form_start(form, {attr: {'novalidate': 'novalidate'}}) }}
                        
                    <div style="color:white;">{{form_label(form.nom,"Name")}}
                            {{form_widget(form.nom)}}
                            {{form_errors(form.nom)}}

                      
                            {{form_label(form.prenom,"lastname")}}
                            {{form_widget(form.prenom)}}
                            {{form_errors(form.prenom)}}

                            {{form_label(form.userName,"userName")}}
                            {{form_widget(form.userName)}}
                            {{form_errors(form.userName)}}

                       
                            {{form_label(form.email,"email")}}
                            {{form_widget(form.email)}}
                            {{form_errors(form.email)}}

                      
                            {{form_label(form.numTel,"Tel")}}
                            {{form_widget(form.numTel)}}
                            {{form_errors(form.numTel)}}

                     
                            {{form_label(form.plainPassword,"password")}}
                            {{form_widget(form.plainPassword)}}
                            {{form_errors(form.plainPassword)}}

                      
                            {{form_label(form.image,"image")}}
                            {{form_widget(form.image)}}
                            {{form_errors(form.image)}}

                       
                        
                        
                        
                        {{ form_row(form.agreeTerms) }}
                     <button type="submit" class="btn btn-primary py-3 w-100 mb-4">{{ button_label|default('Save') }}</button>
                    
                        {{form_end(form)}}
                        {# <form method="post" action="{{ path('app_userD', {'id': user.id}) }}" onsubmit="return confirm('Are you sure you want to delete this account?');">
                                                    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ user.id) }}">
                                                    <button class="btn">Delete</button>
                                                </form> #}
                    </div>
                </div>
            </div>
           
        </div>
    </section>
    <!-- Class Details Section End -->

   

    

    {% endblock %}